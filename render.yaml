services:
  - type: web
    name: stockpulse-pro
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: FINMIND_TOKEN
        sync: false
